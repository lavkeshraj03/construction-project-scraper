services:
  - type: web
    name: construction-project-scraper
    env: python
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
